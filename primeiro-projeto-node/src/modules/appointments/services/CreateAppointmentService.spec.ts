import CreateAppointmentService from './CreateAppointmentService';

describe('CreateAppointment', () => {
  it('shold be able to create a new appointment', () => {
    expect(1 + 2).toBe(3);
  });

  it('shold be able to create two appointment on the same time', () => {
    expect(1 + 2).toBe(3);
  });
});
